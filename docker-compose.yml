services:
  app:
    build: .
    restart: unless-stopped
    env_file: .env
    # bound to localhost only — the host nginx reverse-proxies to it
    ports:
      - "127.0.0.1:8088:8000"
    volumes:
      - ./data:/app/data
